version: '3'

services:
  backend:
    image: ghcr.io/schedool/backend:latest
    container_name: backend
    hostname: backend
    ports:
        - '5000:5000'
    volumes:
        - './data:/backend/data'
