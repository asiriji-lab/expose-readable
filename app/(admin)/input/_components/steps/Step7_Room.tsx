export function Step7_Room({ data, onChange, errors }: { data: any; onChange: (field: string, value: any) => void; errors: {[key: string]: string} }) {
    return(
        <div>
            <h2 className="text-2xl font-semibold mb-6 text-black">Step 7: Room Information</h2>
        </div>
    )
}